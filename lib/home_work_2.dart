import 'package:flutter/material.dart';

class HomeWork2 extends StatelessWidget {
  const HomeWork2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color.fromARGB(255, 209, 204, 202),
        title: const Text('HomeWork2'),
        centerTitle: true,
      ),
      body: const Center(child: Text('HomeWork2')),
    );
  }
}
